spring:
  profiles:
    active: test
  messages:
    basename: messages
    encoding: UTF-8
  thymeleaf:
    cache: false
  jpa:
    open-in-view: false
    defer-datasource-initialization: true
  sql:
    init:
      mode: always
logging:
  level:
    com.example.travelagency: DEBUG
    org.springframework.security: INFO
